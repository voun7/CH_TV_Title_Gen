[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ch_title_gen"
version = "1.0"
requires-python = ">=3.10"
dependencies = ["pycnnum==2.1.0"]
description = "A program that changes chinese tv show titles to english format."
